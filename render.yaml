services:
  - type: web
    name: pinterest-analyzer
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"
    branch: main
    region: oregon
    plan: free
    build:
      pythonVersion: 3.11.9  # ✅ Correct location

